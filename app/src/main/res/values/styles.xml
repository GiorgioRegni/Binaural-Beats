<resources>

    <style name="Theme.BBT" parent="Theme.AppCompat.Light.NoActionBar">
        <!--<item name="android:background">@android:color/black</item>  This line is changing the actionbar color -->
        <!--<item name="android:windowBackground">@android:color/black</item>-->
        <!-- Main theme colors -->
        <!--   your app branding color for the app bar -->
        <item name="colorPrimary">@color/primary</item>
        <!--   darker variant for the status bar and contextual app bars -->
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <!--   theme UI controls like checkboxes and text fields -->
        <item name="colorAccent">@color/accent</item>
    </style>

    <style name="Theme.BBT.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.BBT.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.BBT.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
